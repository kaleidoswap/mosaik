# Rust
/target
**/*.rs.bk
# Cargo.lock is committed — this workspace ships a binary (swap-cli).

# Elements regtest data
/.regtest
*.log

# Simplicity-capable elementsd / electrs / simplex binaries (see README — Install)
/tools

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
/.testnet

# Local env (Esplora URL + Bearer token); .env.example is committed
.env

# Saved offer JSON produced by ./scripts/testnet.sh make-offer
/offer.json

# UI build artifacts
ui/node_modules/
ui/dist/
ui/tsconfig.tsbuildinfo
